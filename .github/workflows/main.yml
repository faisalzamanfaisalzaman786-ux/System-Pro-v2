- name: Build APK with Bubblewrap (non-interactive)
  run: |
    set -x
    bubblewrap init --manifest manifest.json --directory app --jdk 11   # یہ بتاتا ہے کہ JDK 11 استعمال کرو
    cd app
    bubblewrap build --verbose
    cp app-release-signed.apk ../app.apk
