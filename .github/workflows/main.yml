name: Web to APK (web2apk) – No SDK needed

on:
  repository_dispatch:
    types: [build-apk]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install ImageMagick (for icon conversion)
        run: sudo apt-get update && sudo apt-get install -y imagemagick

      - name: Decode and convert icon to PNG
        run: |
          # Save base64 to a file
          echo "${{ github.event.client_payload.app_icon }}" | base64 -d > icon_raw
          # Convert any image to 512x512 PNG (imagemagick handles jpeg, png, etc.)
          convert icon_raw -resize 512x512! icon.png

      - name: Install web2apk
        run: npm install -g web2apk

      - name: Generate APK
        run: |
          web2apk generate \
            --name "${{ github.event.client_payload.app_name }}" \
            --url "${{ github.event.client_payload.app_url }}" \
            --icon icon.png \
            --output app.apk

      - name: Create GitHub Release and Upload APK
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_id }}
          name: "APK Build #${{ github.run_id }}"
          body: "✅ App: ${{ github.event.client_payload.app_name }}\n🌐 URL: ${{ github.event.client_payload.app_url }}"
          files: app.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
